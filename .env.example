# Copie ce fichier en .env.local et remplis les valeurs (projet Supabase > Settings > API)
# Ces deux clés sont publiques (côté navigateur) mais obligatoires.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
